# Warehouse Manager Mobile

Мобильное приложение для автоматизации складских операций, включая:
- Учёт товаров и их категорий
- Регистрацию поставок (приход товара)
- Фиксацию отгрузок (расход товара)
- Ведение списаний по различным причинам (брак, кража, истечение срока годности и др.)

Приложение предназначено для сотрудников склада и менеджеров, обеспечивая точность данных, предотвращение ошибок и удобство взаимодействий.

---

## 🏗️ Архитектура проекта

### **Бэкенд (Spring Boot + PostgreSQL)**
Бэкенд реализован на Java 17 с использованием фреймворка Spring Boot 3 и СУБД PostgreSQL.

#### Основные компоненты:
- **REST API** — предоставляет эндпоинты для всех операций (CRUD, фильтрация, пагинация)
- **JPA / Hibernate** — объектно-реляционное отображение
- **MapStruct** — генерация мапперов между DTO и Entity
- **Validation** — валидация входящих запросов (`@NotNull`, `@Min` и др.)
- **Безопасность** — обработка ошибок, защита от некорректных операций (нельзя списать больше, чем есть на складе)

#### Бизнес-логика:
- При создании поставки — увеличивается остаток товара
- При создании отгрузки/списания — уменьшается остаток
- При удалении записи — остаток восстанавливается (обратная операция)
- Поддержка динамических фильтров: по категориям, поставщикам, причинам списания

#### База данных:
- Схема `warehouse`
- Таблицы: `products`, `categories`, `providers`, `supplies`, `shipments`, `write_offs`
- Связи: ManyToOne между товарами и операциями, ManyToMany
- Индексы для ускорения фильтрации

---

### **Мобильное приложение (Android, Kotlin)**
Клиентская часть разработана на Kotlin в Android Studio с использованием современных архитектурных подходов и библиотек.

#### Архитектура:
- **Clean Architecture** — чёткое разделение на слои: data, domain, presentation
- **MVVM (Model-View-ViewModel)** — связь UI и логики через StateFlow
- **Repository Pattern** — абстракция над источниками данных

#### Используемые технологии:
- **Jetpack:** ViewModel, Lifecycle, Paging (для товаров)
- **Retrofit** — работа с REST API
- **Coroutines + Flow** — асинхронность без блокировок
- **Material Components** — единый дизайн (CardView, TextInputLayout, кнопки)
- **ItemTouchHelper** — реализация свайпов (редактирование/удаление)

#### Особенности реализации:
- **Свайп-жесты:**
  - Вправо — редактирование (товары)
  - Влево — удаление (поставки, отгрузки, списания)
- **Кастомные диалоги** в едином стиле с закруглёнными карточками
- **Фильтрация** с возможностью выбора нескольких параметров
- **Валидация** на всех этапах:
  - Проверка обязательных полей
  - Ограничение количества по остатку
  - Валидация INN (12 цифр), даты (гггг-мм-дд)
- Поддержка светлой и тёмной тем
- Обработка ошибок

---

## 🔧 Интеграция и развёртывание

- Бэкенд запускается через Docker Compose (включает Spring Boot + PostgreSQL)
- Мобильное приложение подключается к локальному или удалённому серверу
- Проект готов к публикации в Google Play

---

## 🚀 Перспективы развития

- Интеграция с камерой для сканирования штрихкодов/QR-кодов
- Генерация отчётов (PDF/Excel)
- Добавление ролевой модели (админ, кладовщик, менеджер)

---

## 🛠️ Технологический стек

### Backend
- **Язык:** Java 17
- **Фреймворк:** Spring Boot 3
- **База данных:** PostgreSQL
- **ORM:** JPA/Hibernate
- **Маппинг:** MapStruct

### Mobile
- **Язык:** Kotlin
- **Архитектура:** Clean Architecture + MVVM
- **Асинхронность:** Coroutines + Flow
- **Сеть:** Retrofit
- **UI:** Material Components
